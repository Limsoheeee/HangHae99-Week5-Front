import { LogoContainer, LogoCover, LogoText } from "../style/Logo.styled";

export const Logo = () => {
  return(
    <LogoContainer>
      <LogoCover>
        <LogoText>to do list</LogoText>
      </LogoCover>
    </LogoContainer>
  )
};
